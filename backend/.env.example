# Variables de entorno para Backend (Hono + Neon)
# Copiar a .env y completar valores

# Conexión a Neon Postgres (obtener en console.neon.tech)
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/dbname?sslmode=require

# Para desarrollo local con wrangler dev
# wrangler dev --env .env